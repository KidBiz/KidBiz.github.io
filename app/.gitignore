# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp

# Local tooling
# .claude/* not .claude/ — git cannot re-include anything inside a fully excluded
# directory, and the skill below documents the code so it belongs in the repo
.claude/*
!.claude/skills/
node_modules/
